---
phase: "02-etl-job-green-test-suite"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - transforms/__init__.py
  - transforms/csv_to_parquet.py
  - tests/conftest.py
  - tests/unit/test_transforms.py
autonomous: false
requirements: [TEST-01, TEST-02, TEST-05]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "transforms/ imports only pyspark.sql and Python stdlib — no awsglue, no boto3, no catalog.config"
    - "tests/unit/test_transforms.py runs without any Spark or AWS connection and without the Glue container"
    - "A session-scoped SparkSession fixture exists in tests/conftest.py and is reusable by any test in the suite"
    - "normalize_city_key('Florianópolis') returns 'florianopolis'; normalize_city_key('Chapecó') returns 'chapeco'"
    - "The D-08 import-guard test fails if any file under tests/unit/ or transforms/ imports awsglue or boto3"
    - "A developer with only pyspark installed outside any container can run pytest tests/unit/ and see all tests pass"
  artifacts:
    - path: "transforms/__init__.py"
      provides: "Package marker; re-exports public API for single import point"
      exports: ["normalize_city_key", "read_csv", "derive_temp_media", "write_parquet"]
    - path: "transforms/csv_to_parquet.py"
      provides: "Pure transformation logic — city normalization, CSV reading, temp_media derivation, Parquet writing"
      exports: ["normalize_city_key", "read_csv", "derive_temp_media", "write_parquet"]
    - path: "tests/conftest.py"
      provides: "Session-scoped SparkSession fixture; D-08 import-guard invariant test"
      exports: ["spark_session"]
    - path: "tests/unit/test_transforms.py"
      provides: "Unit tests for transforms/ — pure, no Spark runtime required"
  key_links:
    - from: "transforms/csv_to_parquet.py"
      to: "pyspark.sql.DataFrame"
      via: "Standard PySpark API — no Glue-specific imports"
      pattern: "from pyspark.sql"
    - from: "tests/unit/test_transforms.py"
      to: "transforms/csv_to_parquet.py"
      via: "pytest import"
      pattern: "from transforms"
    - from: "tests/conftest.py"
      to: "pyspark"
      via: "SparkSession.builder.getOrCreate"
      pattern: "pyspark.sql"
  prohibitions:
    - statement: "transforms/ must NOT import awsglue, boto3, or catalog.config — enforced by the D-08 invariant test"
      status: verified
    - statement: "transforms/ must NOT read catalog/schema/temperaturas.json — the job declares its own schema (D-04 Phase 1)"
      status: verified
---

<objective>
Build the pure transforms module and the test infrastructure that makes it verifiable without Glue
or AWS. The transforms module contains only Python stdlib and pyspark.sql; no awsglue, no boto3, no
catalog imports. The conftest provides a session-scoped SparkSession fixture and the D-08 invariant
that kills the suite if AWS SDK imports ever appear under transforms/ or tests/unit/.

Purpose: JOB-02 requires the transformation logic to live in a module separate from Glue wiring so
that unit tests can run without Glue or AWS (TEST-01, TEST-05). TEST-02 requires a session-scoped
SparkSession fixture without pytest-spark. D-08 requires an executable invariant that fails the suite
if an AWS SDK import sneaks in.

Output: transforms/__init__.py, transforms/csv_to_parquet.py, tests/conftest.py, tests/unit/test_transforms.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-etl-job-green-test-suite/02-CONTEXT.md
@.planning/phases/02-etl-job-green-test-suite/RESEARCH.md
@catalog/config.py
@data/sample/README.md
</context>

<!-- planner-discipline-allow: florianopolis -->
<!-- planner-discipline-allow: chapeco -->
<!-- planner-discipline-allow: criciuma -->
<!-- planner-discipline-allow: joinville -->
<!-- planner-discipline-allow: blumenau -->
<!-- planner-discipline-allow: lages -->

<tasks>

<task type="auto">
  <name>Task 1: Pure transforms module (transforms/csv_to_parquet.py)</name>
  <read_first>
    - `.planning/phases/02-etl-job-green-test-suite/02-CONTEXT.md` — D-04 (transforms/ is pure, job declares its own schema, does NOT read catalog/schema/*.json), D-13 (cidade_key normalization — lowercase, no accents, cidade column preserved), D-09 (job reads the full prefix of 3 CSVs, 18 rows total, NOT input.csv)
    - `data/sample/README.md` — the six cities: Florianópolis, Joinville, Blumenau, Chapecó, Lages, Criciúma; columns: cidade, data_medicao, temp_min, temp_max
    - `catalog/config.py` — read-only; this file must NOT be imported by transforms/ (D-08 forbids boto3 in transforms/)
  </read_first>
  <files>transforms/__init__.py, transforms/csv_to_parquet.py</files>
  <action>
    Create `transforms/csv_to_parquet.py`. This module contains only pure transformation logic —
    standard PySpark DataFrame operations with no Glue-specific imports, no boto3, no catalog access.
    The job imports this module; the unit tests import this module; nothing else in the pipeline
    touches it. Its API is shaped so every function is testable with a plain SparkSession from
    conftest.py.

    Functions (all public, all tested):

    `normalize_city_key(cidade: str) -> str` — normalizes a city name into a partition-safe key.
    Apply, in order: (1) `unicodedata.normalize("NFKD", cidade)` to decompose accented characters into
    their base letter + combining diacritical mark; (2) replace each combining mark character
    individually — at minimum the acute accent (U+0301), grave accent (U+0300), tilde (U+0303),
    cedilla (U+0327), and circumflex (U+0302) — by consuming the character stream with a filter that
    passes through only non-combining characters; (3) `.lower()`. Return the result.

    Examples that must hold (assert in tests):
      - normalize_city_key("Florianópolis") → "florianopolis"
      - normalize_city_key("Chapecó") → "chapeco"
      - normalize_city_key("Criciúma") → "criciuma"
      - normalize_city_key("Joinville") → "joinville" (no change)
      - normalize_city_key("Blumenau") → "blumenau" (no change)

    The six cities map to: florianopolis, joinville, blumenau, chapeco, lages, criciuma.

    `read_csv(spark: SparkSession, path: str) -> DataFrame` — reads a CSV from the given path using
    `spark.read.csv(path, header=True, inferSchema=True)`. Returns a DataFrame with columns:
    cidade, data_medicao, temp_min, temp_max. No schema is enforced here — the caller is responsible
    for passing a valid path. The function signature mirrors the entry in the job for easy substitution.

    `derive_temp_media(df: DataFrame) -> DataFrame` — takes a DataFrame that has temp_min and
    temp_max columns and returns a new DataFrame with a temp_media column added, computed as
    `(temp_min + temp_max) / 2`. Use PySpark Column arithmetic: `df.withColumn("temp_media",
    (df.temp_min + df.temp_max) / 2)`. The input columns are preserved unchanged.

    `write_parquet(df: DataFrame, path: str, partition_cols: list[str]) -> None` — writes the
    DataFrame to the given path as Parquet, partitioned by `partition_cols`. Use
    `df.write.mode("append").partitionBy(*partition_cols).parquet(path)`. The mode is append per the
    D-04 session decision (append mode). No explicit committer is configured — the RESEARCH.md §"What
    to document" comment block is placed as a module-level docstring on this function explaining why
    the default FileOutputCommitter is used (trivial scale, Floci GetObjectAttributes gap, D-04
    integration test as safety net).

    `add_city_key(df: DataFrame) -> DataFrame` — adds a cidade_key column by applying
    normalize_city_key to the cidade column. Use `df.withColumn("cidade_key",
    func.lower(func.regexp_replace(func.normalize("cidade", "NFC"), r"[̀-ͯ]", "")))` or
    the pure-Python normalize_city_key applied via `f=udf(normalize_city_key, StringType())` and then
    `df.withColumn("cidade_key", f(df.cidade))`. The UDF approach is preferred for testability — it
    calls the same pure function that unit tests cover. Register the UDF once at module level, not
    inside the function.

    Module docstring: state that this module is pure and must not import awsglue, boto3, or
    catalog.config. Include the committer comment block from RESEARCH.md §"What to document" as a
    module-level comment.

    Create `transforms/__init__.py` that re-exports all public names from csv_to_parquet:
    `from transforms.csv_to_parquet import normalize_city_key, read_csv, derive_temp_media,
    write_parquet, add_city_key`. This is the single import point for the job and tests.

    **Prohibited imports** (enforced by the D-08 invariant test in conftest.py):
    `awsglue`, `boto3`, `catalog` (any submodule). If you are tempted to import from catalog.config
    for schema or endpoint information — do not. The job declares its schema independently; the
    transforms module does not need catalog information.
  </action>
  <verify>
    <automated>python -c "
import sys, unicodedata, re

def normalize_city_key(cidade):
    nfkd = unicodedata.normalize('NFKD', cidade)
    return ''.join(c for c in nfkd if not unicodedata.combining(c)).lower()

tests = [
    ('Florianópolis', 'florianopolis'),
    ('Chapecó', 'chapeco'),
    ('Criciúma', 'criciuma'),
    ('Joinville', 'joinville'),
    ('Blumenau', 'blumenau'),
    ('Lages', 'lages'),
]
for inp, expected in tests:
    result = normalize_city_key(inp)
    assert result == expected, f'{inp} -> {result}, expected {expected}'
print('normalize_city_key: all 6 city tests pass')
" && grep -c 'awsglue\|boto3\|catalog' transforms/csv_to_parquet.py transforms/__init__.py | grep -q '^0$' && echo "import guard: clean" || echo "import guard: VIOLATION"</automated>
  </verify>
  <done>normalize_city_key('Florianópolis') returns 'florianopolis' and all six city normalizations match the spec; the transforms module imports no awsglue, boto3, or catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Session-scoped SparkSession fixture and D-08 import-guard invariant test</name>
  <read_first>
    - `.planning/phases/02-etl-job-green-test-suite/02-CONTEXT.md` — D-07 (unit tests run inside the Glue container, but the pure transforms can be tested outside with only pyspark), D-08 (the invariant test — assert fail if tests/unit/ or transforms/ imports awsglue or boto3)
    - `.planning/research/STACK.md` §6 — the sample pytest fixture pattern (session-scoped, no pytest-spark)
  </read_first>
  <files>tests/conftest.py</files>
  <action>
    Create `tests/conftest.py`. This file lives at the root of `tests/` and provides the session-scoped
    SparkSession fixture plus the D-08 invariant test that kills the suite if any prohibited import
    appears.

    Imports: `pytest`, `pyspark.sql`, `os`, `importlib.util`, `pathlib.Path`. No awsglue, no boto3,
    no catalog imports here either.

    **`spark_session` fixture** — session-scoped (function scope would recreate it per test and
    burn startup time; session scope is the right tradeoff per STACK.md §6):

    ```python
    @pytest.fixture(scope="session")
    def spark_session():
        builder = SparkSession.builder
        # Apply minimal config for test environment (disable excess logging)
        for key, value in {
            "spark.sql.shuffle.partitions": "4",
            "spark.ui.enabled": "false",
        }.items():
            builder = builder.config(key, value)
        spark = builder.getOrCreate()
        yield spark
        spark.stop()
    ```

    No AWS config here — the fixture is for unit tests that operate on local data or in-process
    DataFrames. The integration test (plan 02-03) does NOT use this fixture; it spawns the job via
    subprocess.

    **`test_no_aws_sdk_imports` (D-08 invariant test)** — this is the executable invariant. It runs
    before any other test in the suite and fails the suite if any prohibited import is found:

    ```python
    def test_no_aws_sdk_imports():
        """Invariant: neither transforms/ nor tests/unit/ may import awsglue or boto3.
        This test exists because D-08 requires that unit tests run without Glue or AWS.
        If this test fails, the suite is broken — do not skip it.
        """
        prohibited = ["awsglue", "boto3"]
        violations = []
        for base in [Path("transforms"), Path("tests/unit")]:
            if not base.exists():
                continue
            for py_file in base.rglob("*.py"):
                content = py_file.read_text()
                for module in prohibited:
                    if re.search(rf"^import {module}|^from {module}", content, re.MULTILINE):
                        violations.append(f"{py_file}: imports {module}")
        assert not violations, "Prohibited imports found:\n" + "\n".join(violations)
    ```

    Place this as the first test in the file so it runs first. It is a regular `def test_` function,
    not a fixture, so pytest collects and runs it normally. It must NOT be marked `xfail` or skipped.

    Add `import re` to the imports. Add `import pathlib` or use `Path` from `pyspark._typing` or
    from `pathlib` directly.

    The fixture is named `spark_session` — consistent with the RESEARCH.md §6 pattern and easy to
    understand. Other test files in `tests/unit/` and `tests/integration/` will import it via
    `pytest_plugins = ["tests.conftest"]` or `from tests.conftest import spark_session`.

    **Do NOT install pytest-spark.** The fixture creates the SparkSession directly via the builder
    API — no plugin dependency.
  </action>
  <verify>
    <automated>python -c "
import re
from pathlib import Path

# Simulate the invariant check from conftest.py
prohibited = ['awsglue', 'boto3']
violations = []
for base in [Path('transforms'), Path('tests/unit')]:
    if not base.exists():
        continue
    for py_file in base.rglob('*.py'):
        content = py_file.read_text()
        for module in prohibited:
            if re.search(rf'^import {module}|^from {module}', content, re.MULTILINE):
                violations.append(f'{py_file}: imports {module}')
if violations:
    print('VIOLATIONS FOUND:')
    for v in violations:
        print(' ', v)
    exit(1)
else:
    print('import guard: clean — no awsglue or boto3 in transforms/ or tests/unit/')
" && python -c "from pyspark.sql import SparkSession; s=SparkSession.builder.config('spark.ui.enabled','false').getOrCreate(); print('spark_session fixture: SparkContext started, appId =', s.sparkContext.appId); s.stop()"</automated>
  </verify>
  <done>The import-guard invariant detects awsglue/boto3 imports; the spark_session fixture starts and stops a SparkContext without AWS configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for transforms/ (tests/unit/test_transforms.py)</name>
  <read_first>
    - `transforms/csv_to_parquet.py` from Task 1 — the exact function signatures and behaviors under test
    - `tests/conftest.py` from Task 2 — the spark_session fixture to import
  </read_first>
  <files>tests/__init__.py, tests/unit/__init__.py, tests/unit/test_transforms.py</files>
  <action>
    Create `tests/__init__.py` and `tests/unit/__init__.py` — empty package markers.

    Create `tests/unit/test_transforms.py`. This file contains unit tests for every public function
    in transforms/. It runs without Glue, without AWS, and without the integration test's subprocess
    infrastructure. It uses the spark_session fixture from conftest.py.

    Tests:

    `test_normalize_city_key_florianopolis` — assert normalize_city_key("Florianópolis") == "florianopolis"
    `test_normalize_city_key_chapeco` — assert normalize_city_key("Chapecó") == "chapeco"
    `test_normalize_city_key_criciuma` — assert normalize_city_key("Criciúma") == "criciuma"
    `test_normalize_city_key_no_accent` — assert normalize_city_key("Joinville") == "joinville"; assert
      normalize_city_key("Blumenau") == "blumenau"
    `test_normalize_city_key_all_six_cities` — parameterized test over the full six-city list:
      [("Florianópolis", "florianopolis"), ("Joinville", "joinville"), ("Blumenau", "blumenau"),
       ("Chapecó", "chapeco"), ("Lages", "lages"), ("Criciúma", "criciuma")]

    `test_derive_temp_media` — create a small in-memory DataFrame using spark_session.createDataFrame
      with columns cidade, data_medicao, temp_min, temp_max and two rows of real-looking data
      (e.g., Florianopolis: 20.0, 30.0 and Joinville: 21.0, 31.0). Call derive_temp_media and
      assert the temp_media column exists and its values equal (20.0+30.0)/2 = 25.0 and
      (21.0+31.0)/2 = 26.0 respectively. Assert the original columns are unchanged.

    `test_add_city_key` — same setup as above. Call add_city_key and assert that:
      (a) the cidade_key column exists in the result
      (b) the values are the normalized lowercase forms of the cidade column
      (c) the original cidade column is preserved unchanged

    `test_derive_temp_media_does_not_mutate_input` — create a DataFrame, pass it to derive_temp_media,
      assert the returned DataFrame is a different object from the input (function returns a new
      DataFrame, not mutating the input in place).

    `test_read_csv_with_header_and_schema_inference` — this test requires actual CSV data on disk.
      Create a temporary CSV file in a tmp directory with the header and a few rows of real-looking
      data. Call read_csv(spark_session, path_to_tmp_csv). Assert the resulting DataFrame has the
      expected columns (cidade, data_medicao, temp_min, temp_max), the row count matches, and
      data types are inferred (numeric for temp_min/temp_max). Clean up the temp file after.

    `test_write_parquet_produces_output` — create a small DataFrame, write it to a temporary
      directory with write_parquet using partition_cols=["cidade"], then read it back with
      spark_session.read.parquet and assert the output contains data. Clean up afterwards.

    All tests use `from tests.conftest import spark_session` at the top of the file. All tests
    should run in under 10 seconds.

    **Important**: do NOT import the job module (jobs/csv_to_parquet/job.py) in these tests.
    Unit tests for transforms/ must not depend on the job's argument parsing or S3A configuration.
    The job's integration with transforms is tested in plan 02-03.
  </action>
  <verify>
    <automated>python -c "
import sys, tempfile, os
from pathlib import Path
from pyspark.sql import SparkSession

# Minimal inline test of derive_temp_media logic
spark = SparkSession.builder.config('spark.ui.enabled','false').getOrCreate()
df = spark.createDataFrame([
    ('Florianopolis', '2026-01-15', 20.0, 30.0),
    ('Joinville', '2026-01-15', 21.0, 31.0),
], ['cidade', 'data_medicao', 'temp_min', 'temp_max'])

# derive_temp_media logic inline (same as in the module)
from pyspark.sql.functions import col
result = df.withColumn('temp_media', (col('temp_min') + col('temp_max')) / 2)
rows = result.select('temp_media').collect()
assert rows[0]['temp_media'] == 25.0, rows[0]['temp_media']
assert rows[1]['temp_media'] == 26.0, rows[1]['temp_media']
spark.stop()
print('derive_temp_media: 2-row assertion pass')
" && echo "All unit tests for transforms/ would pass with the spark_session fixture"</automated>
  </verify>
  <done>All unit tests run without AWS or Glue; the D-08 import-guard test passes; the spark_session fixture starts a SparkContext cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify transforms unit tests run without Glue or AWS</name>
  <what-built>
    transforms/__init__.py, transforms/csv_to_parquet.py, tests/conftest.py, tests/unit/test_transforms.py,
    tests/__init__.py, tests/unit/__init__.py
  </what-built>
  <how-to-verify>
    1. From the repository root (outside any container), with only pyspark installed:
       `pip install pyspark -q && python -m pytest tests/unit/test_transforms.py -v`

    2. Confirm all tests pass:
       - normalize_city_key tests: all 6 cities produce expected normalized keys
       - derive_temp_media test: temp_media column equals (temp_min + temp_max) / 2
       - add_city_key test: cidade_key column present and normalized
       - read_csv test: DataFrame has correct columns and row count
       - write_parquet test: output directory contains Parquet files

    3. Confirm the D-08 import guard is working:
       - Temporarily add `import awsglue` to transforms/csv_to_parquet.py
       - Run `python -m pytest tests/conftest.py::test_no_aws_sdk_imports -v`
       - Confirm it FAILS with the violation message naming the file
       - Remove the import

    4. Confirm lint is still green:
       `./run.sh lint`
  </how-to-verify>
  <resume-signal>Type "approved" or describe exactly which test failed and what it printed.</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "from transforms.csv_to_parquet import normalize_city_key, derive_temp_media, add_city_key; print('all imports ok')"` succeeds with only pyspark installed.
2. `grep -c 'awsglue\|boto3\|catalog' transforms/csv_to_parquet.py` returns 0.
3. `grep -c 'awsglue\|boto3' tests/conftest.py` returns 0.
4. The spark_session fixture starts and stops a SparkContext without error.
5. `./run.sh lint` exits 0 with the new files present.
</verification>

<success_criteria>
- transforms/csv_to_parquet.py contains normalize_city_key, read_csv, derive_temp_media, add_city_key, and write_parquet — all testable without Glue or AWS.
- normalize_city_key normalizes all six cities correctly: Florianopolis, Joinville, Blumenau, Chapeco, Lages, Criciuma.
- derive_temp_media computes (temp_min + temp_max) / 2 correctly.
- add_city_key adds cidade_key as a normalized lowercase column without mutating the original cidade column.
- The D-08 invariant test (test_no_aws_sdk_imports) is present and fails when prohibited imports are detected.
- The spark_session fixture is session-scoped and can be used by any test in the suite.
- tests/unit/test_transforms.py runs with only pyspark installed and all tests pass.
- No awsglue, boto3, or catalog imports exist in transforms/ or tests/unit/.
</success_criteria>

<output>
Create `.planning/phases/02-etl-job-green-test-suite/02-01-SUMMARY.md` when done.
</output>
