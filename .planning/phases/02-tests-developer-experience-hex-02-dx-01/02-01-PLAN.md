---
phase: "02"
phase_name: "Tests & Developer Experience (HEX-02, DX-01)"
objective: "Rewrite tests with mocks and add lint --fix command"
req_ids: "HEX-02.1 - HEX-02.5, DX-01.1 - DX-01.3"
status: "planned"
created: "2026-08-09"
plan_id: "02-01"
wave: 1
blocks: []
autonomous: true
---

# Plan: Phase 02 - Tests & Developer Experience

## Must-Haves

### Truths (Verifiable Outcomes)
1. `./run.sh lint --fix` executa sem erros
2. `pytest tests/unit/ -v` passa com mocks
3. `tests/unit/test_domain/` existe com testes de entidades
4. `tests/unit/test_ports/` existe com testes de contratos

### Artifacts
- `run.sh` - comando lint --fix adicionado
- `tests/unit/test_domain/` - domínio testado
- `tests/unit/test_ports/` - contratos testados
- `tests/unit/test_transforms.py` - mocks adicionados

### Key Links
- `run.sh lint --fix` → ruff.toml
- `test_transforms.py` → domain/entities.py
- `test_ports/` → domain/ports/

---

## Wave 1: lint --fix Command (Tracer)

### DX-01.1: Modify lint command in run.sh

```bash
<file: run.sh>
<action: Modify existing lint command to add --fix flag>
# Change: run_step "ruff check" → run_step "ruff check --fix"
</file>

<verify>
./run.sh lint --fix
</verify>
```

**Done:** `./run.sh lint --fix` executa sem errors

---

## Wave 2: Test Suite Refactoring

### HEX-02.1: Rewrite test_transforms.py with mocks

```bash
<file: tests/unit/test_transforms.py>
<action: Add unittest.mock imports and mock fixtures>
<verify>
pytest tests/unit/test_transforms.py -v
</verify>
</file>

- Import `unittest.mock` and `patch` decorators
- Create mock Spark DataFrame fixtures
- Test `add_city_key()` and `derive_temp_media()` in isolation
- Add edge cases: empty data, null values

**Done:** Testes passam com mocks

---

### HEX-02.2: Create tests/unit/test_domain/

```bash
<file: tests/unit/test_domain/__init__.py>
<action: Create directory and init file>

<file: tests/unit/test_domain/test_entities.py>
<action: Create entity tests>
<verify>
pytest tests/unit/test_domain/ -v
</verify>
</file>
```

Test CsvRecord:
- Test `to_dict()` and `from_dict()`
- Test validation

Test JobResult:
- Test status transitions
- Test to_dict() output

**Done:** `pytest tests/unit/test_domain/ -v` passa

---

### HEX-02.3: Create tests/unit/test_ports/

```bash
<file: tests/unit/test_ports/__init__.py>
<action: Create directory and init file>

<file: tests/unit/test_ports/test_storage_port.py>
<action: Create port contract tests>
<verify>
pytest tests/unit/test_ports/ -v
</verify>
</file>
```

Test StoragePort contract:
- Verify SparkAdapter implements all required methods
- Test method signatures match ABC

**Done:** `pytest tests/unit/test_ports/ -v` passa

---

## Wave 3: Integration Tests (Deferred)

### HEX-02.4 & HEX-02.5: Deferred to Phase 3

These requirements are deferred due to:
- Require Floci container for S3 fixtures
- Complexity beyond phase scope

```bash
<file: .planning/phases/02/02-CONTEXT.md>
<action: Add deferred section>
```

**Deferred:** HEX-02.4, HEX-02.5

---

## Summary

| Task | Requirement | Verification |
|------|-------------|--------------|
| Add --fix to lint | DX-01.1 | `./run.sh lint --fix` |
| Mock test_transforms | HEX-02.1 | `pytest tests/unit/test_transforms.py -v` |
| test_domain/ | HEX-02.2 | `pytest tests/unit/test_domain/ -v` |
| test_ports/ | HEX-02.3 | `pytest tests/unit/test_ports/ -v` |
