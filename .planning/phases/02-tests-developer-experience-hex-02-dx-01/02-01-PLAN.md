---
phase: "02"
phase_name: "Tests & Developer Experience (HEX-02, DX-01)"
objective: "Rewrite tests with mocks and add lint --fix command"
req_ids: "HEX-02.1 - HEX-02.5, DX-01.1 - DX-01.3"
status: "planned"
created: "2026-08-09"
plan_id: "02-01"
wave: 1
blocks: []
autonomous: true
---

# Plan: Phase 02 - Tests & Developer Experience

## Tracer: lint --fix Command

Before expanding to full test suite, verify the lint --fix workflow works end-to-end.

### Tasks

1. **Configure ruff for auto-fix**
   - Add `ruff.toml` or extend `pyproject.toml` with ruff config
   - Enable fixable rules: `F`, `E`, `I` (unused imports, unused vars, isort)
   - Run `./run.sh lint --fix` to verify it works

2. **Add `lint` and `lint --fix` commands to run.sh**
   - Add `lint` command that runs `ruff check .`
   - Add `lint --fix` command that runs `ruff check --fix .`
   - Document in run.sh help

**Verification:** `./run.sh lint --fix` executes without errors

---

## Wave 1: Test Suite Refactoring

### HEX-02.1: Rewrite test_transforms.py with mocks

1. **Add mock imports**
   - Import `unittest.mock` and `patch` decorators
   - Create mock Spark DataFrame fixtures

2. **Rewrite transformation tests**
   - Mock `SparkSession` and DataFrame operations
   - Test `add_city_key()` and `derive_temp_media()` in isolation
   - Add edge cases: empty data, null values

**Verification:** `pytest tests/unit/test_transforms.py -v` passes

---

### HEX-02.2: Create tests/unit/test_domain/

1. **Create domain test file**
   - `tests/unit/test_domain/__init__.py`
   - `tests/unit/test_domain/test_entities.py`

2. **Test CsvRecord entity**
   - Test `to_dict()` and `from_dict()`
   - Test validation (temp_min <= temp_max)

3. **Test JobResult entity**
   - Test status transitions
   - Test to_dict() output

**Verification:** `pytest tests/unit/test_domain/ -v` passes

---

### HEX-02.3: Create tests/unit/test_ports/

1. **Create port contract tests**
   - `tests/unit/test_ports/__init__.py`
   - `tests/unit/test_ports/test_storage_port.py`

2. **Test StoragePort contract**
   - Verify SparkAdapter implements all required methods
   - Test method signatures match ABC

**Verification:** `pytest tests/unit/test_ports/ -v` passes

---

### DX-01.2: Configure ruff for project

1. **Create ruff.toml**
   ```toml
   [tool.ruff]
   target-version = "py311"
   line-length = 100

   [tool.ruff.lint]
   select = ["E", "F", "I", "N", "W"]
   ignore = []

   [tool.ruff.lint.isort]
   known-first-party = ["jobs"]
   ```

2. **Verify ruff runs without errors**
   - Run `./run.sh lint` to check

**Verification:** `ruff check .` produces no errors

---

## Wave 2: Integration Tests (Optional)

### HEX-02.4: Create adapter integration tests

1. **Create S3 fixture** (if time permits)
   - Mock boto3 S3 operations
   - Test read/write cycle

### HEX-02.5: PySpark real tests

1. **Add Glue container tests**
   - Run actual PySpark DataFrame operations
   - Requires Floci container

---

## Summary

| Task | Requirement | Effort |
|------|-------------|--------|
| Add lint --fix to run.sh | DX-01.1 | Low |
| Configure ruff | DX-01.2 | Low |
| Rewrite test_transforms.py | HEX-02.1 | Medium |
| Create test_domain/ | HEX-02.2 | Medium |
| Create test_ports/ | HEX-02.3 | Medium |
| Integration tests | HEX-02.4, HEX-02.5 | High |

## Must-Haves

1. `./run.sh lint --fix` works
2. Unit tests pass with mocks
3. No new lint errors introduced
