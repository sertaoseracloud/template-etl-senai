---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - jobs/csv_to_parquet/job.py
  - run.sh
  - tools/__init__.py
  - tools/s3_upload.py
  - tools/s3_watch.py
  - tests/unit/test_s3_upload.py
  - tests/unit/test_s3_watch.py
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - SIM-01
  - SIM-02
  - SIM-03
  - SIM-04

estimate:
  tokens: 85000
  raw_tokens: 42000
  tasks: 5
  confidence: high

must_haves:
  truths:
    - Job accepts --file-key parameter and processes only that file
    - Job logs CloudWatch-compatible event (file key, size, timestamp)
    - ./run.sh upload <file> uploads to S3 and returns the key
    - ./run.sh watch polls S3 and triggers job with file parameter
    - Full local flow validates: upload -> trigger -> job -> parquet output
    - Documentation explains EventBridge trigger requires real AWS
  artifacts:
    - jobs/csv_to_parquet/job.py (modified with --file-key arg)
    - tools/s3_upload.py (S3 upload function)
    - tools/s3_watch.py (S3 polling and trigger loop)
    - tools/__init__.py (package init)
    - run.sh (with upload and watch subcommands)
  key_links:
    - job.py -> tools/s3_upload.py (both use catalog/config.py for S3 client)
    - run.sh -> docker compose -> tools container -> s3_upload/s3_watch
    - s3_watch.py -> job.py (triggers job via spark-submit)
---

<objective>
Enable local event-driven ETL simulation: the Glue job accepts a `--file-key` parameter, `./run.sh upload` uploads files to S3, and `./run.sh watch` polls S3 and triggers the job. This validates the complete upload -> trigger -> job -> parquet output flow in Floci.
</objective>

<context>
@jobs/csv_to_parquet/job.py
@run.sh
@catalog/seed.py
@catalog/config.py
@docker-compose.yml
</context>

<tasks>

<task type="tracer">
  <name>End-to-end local trigger simulation flow</name>
  <files>
    jobs/csv_to_parquet/job.py
    run.sh
    tools/s3_upload.py
    tools/s3_watch.py
    tools/__init__.py
  </files>
  <action>
    Implement the complete local trigger simulation:

    1. Modify jobs/csv_to_parquet/job.py:
       - Add `--file-key` CLI argument (argparse, not required per D-01)
       - Implement env var fallback: `file_key = args.file_key if args.file_key else os.environ.get("FILE_KEY")`
       - If file_key is provided, construct single-file path: `raw_path = f"s3a://{raw_bucket}/{file_key}"`
       - If file does not exist in S3, log "File {file_key} not found, skipping silently." and `sys.exit(0)` per D-02
       - If file_key is None, fall back to default batch path: `raw_path = f"s3a://{raw_bucket}/temperaturas/"`
       - Add CloudWatch-compatible trigger event logging per EVT-02:
         ```python
         logger.info(f"TRIGGER_EVENT: {{'file_key': '{file_key}', 'size_bytes': {size}, 'timestamp': '{iso_timestamp}'}}")
         ```
         Use GlueContext logger (awsglue.context.GlueContext) for CloudWatch-compatible output.

    2. Create tools/__init__.py:
       - Empty file to make tools/ a package

    3. Create tools/s3_upload.py:
       - Function `upload_file(local_path: str) -> str`: uploads file to S3, returns the S3 key
       - Key format: `temperaturas/{filename}` (follows seed.py pattern)
       - Print key to stdout after successful upload
       - Use `catalog/config.s3_client()` for boto3 client (endpoint already configured)

    4. Create tools/s3_watch.py:
       - `POLL_INTERVAL` from env var, default 5 seconds (per D-04)
       - `PROCESSED_FILE` tracking via `/tmp/processed_files.txt`
       - `load_processed_files()`: load set of processed file keys
       - `save_processed_file(key)`: record processed key
       - `poll_and_trigger()`: list_objects_v2 on temperaturas/ prefix, trigger new files
       - `trigger_job(file_key)`: invoke Glue job via docker compose with `--file-key` parameter
         ```python
         cmd = ["docker", "compose", "--profile", "glue", "run", "--rm", "glue",
                "spark-submit", "jobs/csv_to_parquet/job.py", "--JOB_NAME", "csv_to_parquet",
                "--file-key", file_key]
         ```
       - `watch_loop()`: infinite polling loop with Ctrl+C handling

    5. Modify run.sh:
       - Add `upload` and `watch` to the case statement (line 205-206)
       - Add `cmd_upload()` function following cmd_seed pattern:
         ```bash
         cmd_upload() {
           local file="$1"
           preflight
           require_file "$file" "File not found: $file"
           run_step "upload $file" docker compose --profile tools run --rm tools python -c "
         import sys
         sys.path.insert(0, '/workspace')
         from tools.s3_upload import upload_file
         print(upload_file('$file'))
         "
         }
         ```
       - Add `cmd_watch()` function:
         ```bash
         cmd_watch() {
           preflight
           echo "Watching S3 for new files (poll interval: ${POLL_INTERVAL:-5}s)..."
           docker compose --profile tools run --rm tools python -c "
         import sys
         sys.path.insert(0, '/workspace')
         from tools.s3_watch import watch_loop
         watch_loop()
         "
         }
         ```
       - Update usage() to list upload and watch subcommands
  </action>
  <verify>
    <automated>
      # 1. Test job accepts --file-key (manual test required - Floci doesn't support job execution)
      # 2. Test upload subcommand
      docker compose --profile tools run --rm tools python -c "from tools.s3_upload import upload_file; print(upload_file('data/sample/test.csv'))"
      # 3. Test s3_watch import
      docker compose --profile tools run --rm tools python -c "from tools.s3_watch import watch_loop, trigger_job; print('imports ok')"
      # 4. Verify run.sh accepts new subcommands
      ./run.sh upload --help 2>&1 | grep -q "File not found" || true
      ./run.sh watch --help 2>&1 | grep -q "poll" || true
    </automated>
  </verify>
  <done>
    ./run.sh upload data/sample/test.csv returns S3 key, ./run.sh watch starts polling loop, and job.py --file-key triggers single-file processing
  </done>
</task>

<task type="auto">
  <name>Write unit tests for s3_upload and s3_watch</name>
  <files>
    tests/unit/test_s3_upload.py
    tests/unit/test_s3_watch.py
  </files>
  <action>
    Create tests/unit/test_s3_upload.py:
    - Test `upload_file()` returns the correct S3 key format (temperaturas/{filename})
    - Test `upload_file()` calls s3.upload_file with correct bucket and key
    - Mock boto3 s3_client and verify upload_file calls upload_file() with correct args
    - DO NOT import boto3 in test file (D-08: no AWS SDK in unit tests)

    Create tests/unit/test_s3_watch.py:
    - Test `load_processed_files()` returns empty set when file doesn't exist
    - Test `save_processed_file()` creates file with correct content
    - Test `poll_and_trigger()` filters already-processed files
    - Mock boto3 s3_client.list_objects_v2 and docker subprocess
    - Test trigger_job() constructs correct docker compose command
    - DO NOT import boto3 in test file - use unittest.mock.patch
  </action>
  <verify>
    <automated>docker compose --profile tools run --rm tools python -m pytest tests/unit/test_s3_upload.py tests/unit/test_s3_watch.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All unit tests pass for s3_upload and s3_watch modules
  </done>
</task>

<task type="auto">
  <name>Add SIM-04 documentation about EventBridge Floci limitation</name>
  <files>
    README.md
  </files>
  <action>
    Update README.md to add a section explaining the EventBridge trigger limitation:
    - Section: "Event-Driven Triggers (EventBridge)"
    - Explain that Floci does NOT support EventBridge service
    - Local development uses `./run.sh watch` polling as a workaround
    - Production deployment requires real AWS with EventBridge rules
    - Document the trigger mechanism: S3 ObjectCreated -> EventBridge rule -> Glue job with --file-key parameter
  </action>
  <verify>
    <automated>grep -c "EventBridge" README.md && grep -c "Floci does NOT support" README.md</automated>
  </verify>
  <done>
    README.md documents that EventBridge triggers require real AWS and explains the Floci polling workaround
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tools container -> S3 | boto3 calls from tools container to Floci emulator |
| s3_watch.py -> docker compose | Subprocess invocation of docker compose from Python |
| Floci S3 -> Glue job | S3 stores raw CSV, Glue reads via S3A |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | S3 file key logged to stdout | low | accept | File keys are not sensitive; logging is intentional for debugging |
| T-05-02 | Denial of Service | watch loop never exits on S3 error | medium | mitigate | Add error handling with retry limit and exit on persistent failures |
| T-05-03 | Tampering | Command injection in trigger_job subprocess | low | mitigate | Use list-based subprocess args, not shell string interpolation |

## Package Legitimacy Audit

No new package installs in this plan. boto3 and ruff are already in requirements.txt.

</threat_model>

<verification>
## Phase 5 Plan 1 Verification

1. `./run.sh upload data/sample/test.csv` uploads file and prints S3 key to stdout
2. `./run.sh watch` starts polling loop (press Ctrl+C to exit)
3. Job accepts `--file-key` and processes only that file
4. Job logs CloudWatch-compatible trigger event with file key, size, timestamp
5. Documentation explains EventBridge requires real AWS
</verification>

<success_criteria>
- [ ] job.py accepts --file-key CLI arg with FILE_KEY env var fallback
- [ ] job.py exits 0 silently when file key points to non-existent S3 object
- [ ] job.py logs CloudWatch-compatible trigger event
- [ ] tools/s3_upload.py uploads files to S3 with temperaturas/ prefix
- [ ] tools/s3_watch.py polls S3 and triggers job via docker compose
- [ ] run.sh upload subcommand works
- [ ] run.sh watch subcommand works
- [ ] Unit tests pass for s3_upload and s3_watch
- [ ] README documents EventBridge Floci limitation
</success_criteria>

<output>
Create `.planning/phases/05-event-trigger-local-simulation/05-01-SUMMARY.md` when done
</output>
