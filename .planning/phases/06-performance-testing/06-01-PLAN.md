---
phase: "06"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified:
  - "run.sh"
autonomous: true
requirements:
  - "PERF-01"
  - "PERF-02"
  - "PERF-03"
  - "PERF-04"
  - "PERF-05"
  - "EVT-03"
  - "EVT-04"
  - "EVT-05"

estimate:
  tokens: 25000
  raw_tokens: 18000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "scripts/generate_test_data.py generates CSV with N rows matching temperaturas schema"
    - "./run.sh perf-test N runs generate -> upload -> job pipeline end-to-end"
    - "Performance test logs execution time and throughput in rows/second"
    - "Results are logged in structured JSON format to results/perf-TIMESTAMP.json"
    - "Terraform EventBridge infrastructure (EVT-03, EVT-04, EVT-05) validated from Phase 5"
  artifacts:
    - "scripts/generate_test_data.py"
    - "tests/unit/test_generate_test_data.py"
    - "tests/integration/test_perf_test.py"
  key_links:
    - "generate_test_data.py normalize_key() must match transforms.normalize_city_key() exactly"
    - "run.sh perf-test must wire to existing s3_upload.upload_file() and job.py --file-key"
---

# Phase 6 Plan 1: Performance Testing Infrastructure

## Goal

Dynamic test data generator creates configurable datasets, performance tests measure throughput and log structured results.

**As a** developer
**I want to** generate synthetic test data and run performance benchmarks
**so that** I can validate ETL pipeline throughput and measure rows/second metrics

## Phase Requirements

| ID | Requirement | Test Coverage |
|----|-------------|---------------|
| PERF-01 | Script generates CSV with configurable number of rows | `test_cli_rows_output` |
| PERF-02 | Supports --rows, --output, --schema matching existing schema | `test_schema_compliance` |
| PERF-03 | `./run.sh perf-test N` runs full pipeline with generated data | `test_perf_test_integration` |
| PERF-04 | Logs execution time and throughput (rows/second) | `test_timing_logged` |
| PERF-05 | Results in structured JSON format | `test_json_format` |
| EVT-03 | EventBridge rule targets Glue job (Phase 5, validated here) | Documentation |
| EVT-04 | Input Transformer extracts S3 key (Phase 5, validated here) | Documentation |
| EVT-05 | IAM policy restricts to specific job (Phase 5, validated here) | Documentation |

## Execution Context

@$HOME/.claude/gsd-core/workflows/execute-plan.md

## Context

@.planning/ROADMAP.md
@.planning/phases/06-performance-testing/RESEARCH.md
@transforms/csv_to_parquet.py
@tools/s3_upload.py
@jobs/csv_to_parquet/job.py

## Tasks

<task type="tracer">
  <name>End-to-end performance test flow</name>
  <files>
    scripts/generate_test_data.py
    run.sh
    tests/integration/test_perf_test.py
  </files>
  <action>
Create `scripts/generate_test_data.py` that:
1. Accepts `--rows N` (required), `--output PATH` (required), `--date YYYY-MM-DD` (default: 2026-01-15)
2. Generates CSV with columns: cidade, cidade_key, data_medicao, temp_min, temp_max
3. Uses 6 cities: Florianopolis, Joinville, Blumenau, Chapeco, Lages, Criciuma (with accents)
4. Implements normalize_key() matching transforms.csv_to_parquet.normalize_city_key() exactly:
   ```python
   unicodedata.normalize("NFKD", cidade) -> filter combining chars -> lower()
   ```
5. Generates temp_min (10.0-25.0), temp_max (20.0-35.0, always >= temp_min)
6. Writes using csv.writer (stdlib only, no pandas)

Update `run.sh`:
1. Add `perf-test` to case statement allowed subcommands (line 236)
2. Add `cmd_perf_test()` function that:
   - Takes n_rows as positional argument
   - Generates temp CSV to mktemp
   - Uploads via s3_upload.upload_file()
   - Runs job.py with --file-key pointing to uploaded file
   - Measures end-to-end timing with time.perf_counter()
   - Logs structured JSON to results/perf-TIMESTAMP.json with fields:
     - test, rows_generated, elapsed_seconds, throughput_rows_per_sec, timestamp, s3_key
   - Prints result to stdout for CI capture

Create `tests/integration/test_perf_test.py` that verifies:
- `./run.sh perf-test 100` completes without error
- results/perf-*.json exists and contains required fields
</action>
<behavior>
  - Test: `python scripts/generate_test_data.py --rows 100 --output /tmp/test.csv` creates file with 100 data rows
  - Test: CSV header is "cidade,cidade_key,data_medicao,temp_min,temp_max"
  - Test: cidade_key for "Florianopolis" is "florianopolis" (matches normalize_city_key)
  - Test: temp_max >= temp_min for all rows
  - Test: `./run.sh perf-test 100` exits 0 and produces JSON with throughput field
  - Test: JSON contains rows_generated, elapsed_seconds, throughput_rows_per_sec
</behavior>
<verify>
  <automated>pytest tests/unit/test_generate_test_data.py -x && pytest tests/integration/test_perf_test.py -x</automated>
</verify>
<done>generate -> upload -> job -> JSON result pipeline works end-to-end with 100 rows</done>
</task>

<task type="auto">
  <name>Unit tests for generate_test_data.py</name>
  <files>
    tests/unit/test_generate_test_data.py
    scripts/generate_test_data.py
  </files>
  <action>
Create `tests/unit/test_generate_test_data.py` with the following test cases:

1. `test_cli_rows_output` - Verify script accepts --rows and --output args
2. `test_schema_compliance` - Verify output has correct columns and cidade_key format
3. `test_normalize_key_matches_transforms` - Verify normalize_key() output matches transforms.csv_to_parquet.normalize_city_key() for all 6 cities
4. `test_temp_range` - Verify temp_min in [10.0, 25.0] and temp_max in [20.0, 35.0]
5. `test_temp_max_gte_min` - Verify temp_max >= temp_min for all generated rows
6. `test_json_format` - Verify timing results have correct JSON structure
7. `test_date_format` - Verify --date parameter produces YYYY-MM-DD format

Use unittest.mock to test CLI parsing without running the script. Use subprocess to run generate_test_data.py for schema validation tests.

Per D-08 invariant, tests must NOT import awsglue or boto3.
</action>
<verify>
  <automated>pytest tests/unit/test_generate_test_data.py -x -v</automated>
</verify>
<done>All 7 unit tests pass: CLI args, schema compliance, normalize_key, temp ranges, JSON format</done>
</task>

<task type="auto">
  <name>Phase 5 EventBridge documentation validation</name>
  <files>
    terraform/modules/eventbridge/main.tf
    terraform/modules/eventbridge/variables.tf
    terraform/modules/eventbridge/outputs.tf
  </files>
  <action>
Phase 5 already implemented the EventBridge infrastructure (EVT-03, EVT-04, EVT-05). This task validates and documents that infrastructure for completeness:

1. Verify terraform/modules/eventbridge/main.tf contains:
   - CloudTrail-based S3 ObjectCreated event pattern
   - EventBridge rule targeting Glue job
   - Input Transformer extracting S3 key

2. Verify terraform/modules/eventbridge/variables.tf defines:
   - project_name, raw_bucket_name, glue_job_arn, prefix

3. Verify terraform/modules/eventbridge/outputs.tf exports eventbridge_role_arn

4. Add inline documentation comments explaining how local perf-test validates the trigger mechanism (local watch loop demonstrates same flow as EventBridge)

5. Create terraform/modules/eventbridge/VALIDATION.md documenting:
   - How to test EventBridge locally via ./run.sh watch
   - How EventBridge would trigger in real AWS
   - Input Transformer JSONPath expressions used

This satisfies requirement #8: "Local performance test validates the trigger mechanism end-to-end"
</action>
<verify>
  <automated>grep -l "aws_cloudwatch_event_rule" terraform/modules/eventbridge/main.tf && grep -l "input_transformer" terraform/modules/eventbridge/main.tf</automated>
</verify>
<done>EventBridge infrastructure documented; terraform modules verified present</done>
</task>

</tasks>

## Dependency Graph

```
Task 1: End-to-end performance test flow (TRACER)
  needs: transforms/csv_to_parquet.py (normalize_city_key), tools/s3_upload.py, job.py
  creates: scripts/generate_test_data.py, updates run.sh
  wave: 1

Task 2: Unit tests
  needs: scripts/generate_test_data.py (from Task 1)
  creates: tests/unit/test_generate_test_data.py
  wave: 1 (parallel with Task 1 if generator exists)

Task 3: EventBridge documentation
  needs: terraform/modules/eventbridge/ (Phase 5)
  creates: terraform/modules/eventbridge/VALIDATION.md
  wave: 1 (independent)
```

## Threat Model

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | generate_test_data.py | low | accept | Synthetic test data only; no real user data |
| T-06-02 | Denial of Service | CSV generation | low | mitigate | Cap rows at 10M via argparse type validation (positive int) |
| T-06-03 | Path Traversal | --output parameter | low | mitigate | Path().parent.mkdir() is safe; output is local temp |
| T-06-04 | Amplification | Large row count | medium | accept | User-controlled; 100K rows ~5MB CSV is reasonable |

## Verification

After all tasks complete:

1. `python scripts/generate_test_data.py --rows 1000 --output /tmp/perf_1k.csv` produces 1000-row CSV
2. `./run.sh perf-test 100` outputs JSON with throughput field
3. `pytest tests/unit/test_generate_test_data.py -x` all pass
4. `pytest tests/integration/test_perf_test.py -x` passes
5. `ls results/perf-*.json` shows timestamped result files

## Success Criteria

- [ ] `scripts/generate_test_data.py --rows N --output <path>` creates CSV with N rows matching existing schema
- [ ] `./run.sh perf-test <n_rows>` runs full pipeline with generated data
- [ ] Performance test logs execution time and throughput (rows/second)
- [ ] Results logged in structured JSON format
- [ ] EventBridge infrastructure (EVT-03, EVT-04, EVT-05) from Phase 5 validated
- [ ] Unit tests cover generator and timing logic
- [ ] Integration test validates end-to-end flow

## Output

Create `.planning/phases/06-performance-testing/06-01-SUMMARY.md` when done.
